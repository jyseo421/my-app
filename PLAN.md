# PLAN.md

## 이번 사이클 목표

`PRD.md`에 정의된 두 가지 Must 기능(자동 일정 수립, 모델 정보 입력/관리)을 모두 구현하여, 신모델 개발일정 담당자가 50개 이상 모델의 개발 순서와 일정을 자동으로 산출할 수 있는 시스템을 완성한다.

## 성공 기준

- 담당자 1인이 50개 이상 모델의 개발 일정을 하루 이내에 산출할 수 있다.
- 법인별 주간 Capa를 초과하는 일정 오류가 0건이다.
- 모델 조건(그룹/출향지/법인 등) 변경 시 재계산이 수 분 이내에 끝난다.
- `PRD.md` 5절의 Must 기능(자동 일정 수립, 모델 정보 입력/관리) 규칙이 빠짐없이 동작한다.

## 작업 순서

1. Next.js 프로젝트 초기화(App Router) 및 Vercel 배포 파이프라인 연결 *(PRD 개발단위 1)*
2. Supabase 프로젝트 생성 및 Next.js 연동(환경변수, 클라이언트 설정) *(PRD 개발단위 1)*
3. 단일 비밀번호 접근 통제 게이트 구현(최초 설정/변경 포함) *(PRD 개발단위 2)*
4. DB 스키마 설계·마이그레이션 — 법인, 사양그룹, 신뢰성 시험 마스터, 출향지·규격 마스터, 모델, 모델-시험 매핑(model_test, 모델당 동일 항목 중복 방지 UNIQUE 제약), 일정 계산 결과(schedule_run/schedule_result), 앱 설정(app_setting: 비밀번호 해시) 테이블 포함 *(공통 인프라)*
5. 법인 마스터(법인명, 주간 Capa) 등록/조회/수정/삭제 기능 구현 *(PRD 개발단위 3)*
6. 사양그룹 마스터(그룹명, 기구·성능개발 병렬/직렬 설정, 우선순위, 기구개발·성능개발 소요시간) 등록/조회/수정/삭제 기능 구현 *(PRD 개발단위 4)*
7. 신뢰성 시험 마스터(항목명, 소요시간, 병렬/직렬 여부) 등록/조회/수정/삭제 기능 구현 *(PRD 개발단위 5)*
8. 출향지·규격 마스터(출향지명, 우선순위, 규격취득 소요시간) 등록/조회/수정/삭제 기능 구현 — 출향지 우선순위는 법인 Capa 초과 시 2차 정렬 기준으로 사용 *(PRD 개발단위 6)*
9. 모델 정보 입력 화면(그룹, 출향지, 법인, 규격취득 필요 여부, 신뢰성 시험 항목 다중 선택) 구현 — 모델 자체에는 우선순위와 기구/성능개발 소요시간을 두지 않고, 정렬은 소속 사양그룹/출향지의 우선순위를, 소요시간은 소속 사양그룹의 값을 참조 *(PRD 개발단위 7, 8)*
10. 모델 필수 항목 누락 검증(등록·실행 차단)과 동일 시험 항목 중복 입력 에러 처리 구현 *(PRD 개발단위 10, 11)*
11. 엑셀 붙여넣기(Ctrl+V)/파일 첨부를 통한 마스터·모델 데이터 일괄 입력 기능 구현 — 모델 정보뿐 아니라 법인/사양그룹/시험/규격 마스터 4종 화면에도 동일하게 적용 *(PRD 개발단위 9)*
12. 일정 시작 기준일 입력 및 한국 공휴일 캘린더 반영 로직 구현 *(PRD 개발단위 12)*
13. 사양그룹 병렬/직렬 설정 기반 기구개발·성능개발 소요시간 계산 로직 구현 *(PRD 개발단위 13)*
14. 신뢰성 시험·규격취득 병렬 진행 소요시간 계산 및 개발 완료 판정(월요일 시작 기준 주차 산정) 로직 구현 *(PRD 개발단위 14)*
15. 법인 Capa 기반 주간 완료 모델 수 제한, 초과 시 우선순위(사양그룹→출향지) 기반 재배치·재집계 로직 구현 *(PRD 개발단위 15)*
16. 전체 모델 자동 일정 수립 실행 기능 및 결과 목록 화면 구현 — 50개 모델 기준 재계산 소요 시간을 실측해 "수 분 이내" 목표 충족 여부 확인 포함 *(PRD 개발단위 16)*
17. 조건 변경 시 전체 재계산과 이전/이후 일정 비교(변경분 색상 표시) 화면 구현 — 일정 결과는 계산 실행(run) 단위로 저장하고, 비교 화면은 최신 run과 직전 run을 대조 *(PRD 개발단위 17)*
18. 일정 결과·모델 정보·마스터 테이블 엑셀 내보내기 기능 구현 *(PRD 개발단위 18)*
