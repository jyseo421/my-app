# DESIGN.md

> `PLAN.md`의 18단계 작업을 실제로 구현하기 위한 설계 문서. PC/데스크탑 화면 기준(모바일 대응 제외), 차분한 블루/네이비 계열 업무용 대시보드 톤(`prd_lite.md` 5절 기준).

## 1. 화면 구성

| 화면 | 무엇이 보이는가 | 관련 PLAN 단계 |
|------|----------------|----------------|
| **비밀번호 게이트** | 앱 진입 시 비밀번호 입력창 하나만 보임. 최초 실행 시에는 "비밀번호 설정" 화면으로 전환됨. 로그인 후 화면 우측 상단 메뉴에서 "비밀번호 변경" 가능 | 3 |
| **마스터 관리 (법인/사양그룹/시험/규격·출향지)** | 좌측에 4개 탭(법인, 사양그룹, 신뢰성 시험, 규격·출향지), 우측에 선택한 탭의 표(목록) + "추가/수정" 폼. 법인 탭은 법인명·주간 Capa, 사양그룹 탭은 그룹명·병렬-직렬 설정·우선순위·**기구개발 소요일·성능개발 소요일**, 시험 탭은 항목명·소요일·병렬-직렬, 규격·출향지 탭은 출향지명·우선순위·규격취득 소요주(週). 각 탭 상단에는 "엑셀 붙여넣기(Ctrl+V)/파일 첨부"와 "엑셀 다운로드" 버튼이 동일하게 있음 | 5, 6, 7, 8, 11 |
| **모델 정보 관리** | 상단에 전체 모델 표(그룹/출향지/법인/규격취득 여부 컬럼), 표 위에 "새 모델 추가" 버튼과 "엑셀 붙여넣기/파일 첨부" 버튼. 모델 클릭 시 우측에 상세 입력 폼(필수 항목은 빨간 별표, 미입력 시 저장 버튼 비활성화). 법인을 선택하면 해당 법인의 주간 Capa가, 사양그룹을 선택하면 그 그룹의 기구·성능개발 소요일이 각각 읽기 전용으로 자동 표시됨(소요일 자체는 사양그룹 마스터에서만 입력). 신뢰성 시험 항목은 체크박스로 다중 선택. 일괄 등록 시 필수 항목 누락 모델이 있으면 경고 배너를 띄우고 전체 등록을 막으며, 동일 시험 항목이 중복 입력되면 에러 팝업을 띄움 | 9, 10, 11 |
| **일정 수립 실행** | 상단에 "일정 시작 기준일" 날짜 선택기와 "자동 일정 수립 실행" 버튼. 필수 항목이 비어있는 모델이 하나라도 있으면 실행 버튼이 비활성화되고 누락 모델 목록을 배너로 표시. 실행 후 하단에 모델별 개발 순서·기간을 표 형태로 표시(간트차트는 이후 확장 검토), 법인별 주간 완료 모델 수를 요약 카드로 상단에 표시. Capa 제약으로 일정이 길어져도 결과 행은 잘라내지 않고 전량 표시 | 12, 13, 14, 15, 16 |
| **일정 비교 화면** | 조건 변경 후 "재계산" 실행 시, 좌측에 이전 일정 표·우측에 변경된 일정 표를 나란히 배치. 날짜나 순서가 바뀐 셀은 배경색(예: 노란색)으로 강조. 비교 대상은 계산 실행(run) 단위로 저장된 최신 run과 직전 run | 17 |
| **엑셀 내보내기** | 별도 화면이 아니라 마스터 관리/모델 관리/일정 수립 화면 각각에 "엑셀 다운로드" 버튼으로 존재 | 18 |

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력]
  담당자가 화면에서 입력하거나 엑셀로 붙여넣는 데이터
  - 마스터 데이터: 법인(Capa), 사양그룹(병렬/직렬, 우선순위, 기구·성능개발
                   소요시간(일)), 시험 항목, 출향지(우선순위)·규격(출향지별 소요주)
  - 모델 데이터: 그룹/출향지/법인/규격취득 필요 여부
                 (모델 자체에는 우선순위나 기구·성능개발 소요시간을 두지 않고,
                  소속 사양그룹의 값과 우선순위, 출향지의 우선순위를 그대로 참조)
  - 일정 시작 기준일
        │
        ▼  (Next.js 화면 → API Route 호출, 저장은 Supabase에 즉시 반영)
[저장]
  Supabase(PostgreSQL) 테이블에 마스터/모델 데이터 저장
  비밀번호 해시는 app_setting 테이블에 저장
        │
        ▼  ("자동 일정 수립 실행" 클릭 시 서버에서 계산, 결과는 schedule_run/
            schedule_result 테이블에 실행(run) 단위로 저장)
[처리 — 일정 계산 엔진 (Next.js API Route)]
  1. 모델별 기구/성능개발 소요시간 계산 (소요시간은 모델이 속한 사양그룹에
     설정된 값을 사용, 사양그룹 병렬/직렬 규칙 적용, 공휴일·주말을 제외한
     실제 근무일 기준으로 날짜 환산)
  2. 신뢰성 시험 소요시간 계산 (기본 병렬 + 일부 직렬 항목 반영,
     공휴일·주말 제외한 근무일 기준)
  3. 규격취득 소요시간 계산 — "규격취득 필요=Y" 표기된 모델만 출향지별
     소요주(週)를 캘린더 주 그대로 가산(공휴일 미제외). N인 모델은
     규격취득 기간 없이 신뢰성 시험 종료일이 완료일 후보가 됨
  4. 신뢰성 시험 종료일과 규격취득 종료일 중 늦은 날짜를 완료일로 확정,
     월요일 시작 기준(ISO week)으로 완료 주를 산정
  5. 확정된 완료 주 기준으로 법인 주간 Capa 초과 여부 확인
  6. 초과 시 사양그룹 우선순위 → 동일 그룹 내 출향지 우선순위 순으로
     낮은 우선순위 모델의 완료 주를 다음 주로 이월하고, 이월된 주에서
     다시 5)를 반복 (더 이상 초과가 없을 때까지 수렴)
        │
        ▼
[출력]
  - 화면: 모델별 개발 순서·일정 표, 법인별 주간 완료 현황
    (Capa로 일정이 길어져도 결과 행은 제한 없이 전량 표시)
  - 재계산 시: 이전 vs 이후 비교 화면(변경분 색상 표시, 최신 run ↔ 직전 run)
  - 엑셀 파일: 일정 결과 / 모델 정보 / 마스터 테이블 다운로드
```

## 3. 기술 선택

기본은 `PLAN.md`/`PRD.md`에서 이미 정한 대로 **Next.js**이고, Vercel로 그대로 배포합니다. 그 위에 이 프로젝트를 만들려면 아래 기술이 추가로 필요합니다.

| 기술 | 쉬운 설명 1줄 |
|------|---------------|
| **Supabase (PostgreSQL)** | 엑셀 대신 데이터를 안전하게 저장해두는 온라인 데이터베이스 (이미 PRD에서 정한 선택) |
| **Tailwind CSS + shadcn/ui** | 표·버튼·입력창 같은 화면 부품을 예쁘게 미리 만들어둔 재료 모음. 매번 디자인을 새로 그리지 않아도 깔끔한 업무용 화면을 빨리 만들 수 있음 |
| **SheetJS (xlsx 라이브러리)** | 엑셀 붙여넣기(Ctrl+V)나 파일 업로드 내용을 읽고, 반대로 화면 데이터를 엑셀 파일로 만들어 다운로드하게 해주는 도구 |
| **date-fns** | 날짜끼리 더하고 빼고 "며칠 후"를 계산하는 걸 실수 없이 도와주는 날짜 계산 도구 |
| **date-holidays** | 한국 공휴일 날짜가 미리 들어있는 도구. 별도 API 키나 인터넷 연결 없이도 "이 날이 공휴일인지" 바로 확인할 수 있음 |
| **TanStack Query (React Query)** | 화면이 서버(데이터베이스)에서 최신 데이터를 가져오고, 저장한 내용을 다시 불러오는 과정을 자동으로 관리해주는 도구. 새로고침 없이 화면이 최신 상태를 유지하게 해줌 |
| **bcrypt** | 담당자가 설정한 비밀번호를 그대로 저장하지 않고 암호화된 형태로 안전하게 저장/확인해주는 도구 |
