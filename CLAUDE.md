# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 작업 규칙

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

코드를 변경할 때마다 아래 절차를 통과할 때까지 반복한다.

1. **변경한다** — 계획한 코드 변경을 적용한다.
2. **결과를 직접 확인한다** — 브라우저로 열기/실행 등 실제 동작을 눈으로 확인한다. 확인 없이 완료로 간주하지 않는다.
3. **스스로 코드 리뷰한다** — 변경한 코드를 다시 읽으며 버그, 규칙 위반, 불필요한 변경이 없는지 점검한다.
4. **문제가 있으면 고치고 다시 1)로 돌아간다** — 통과할 때까지 루프를 반복한다.
5. 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다.

## 프로젝트 현재 상태

이 저장소는 아직 코드가 작성되지 않은 **기획 단계**입니다. `PRD.md`(상세 기획서)와 `prd_lite.md`(요약본)만 존재하며, Next.js 프로젝트 스캐폴딩도 아직 되어 있지 않습니다. 실제 구현이 시작되면 이 파일에 빌드/린트/테스트/단일 테스트 실행 명령어를 추가해야 합니다.

두 기획서 중 `PRD.md`가 더 상세하고 최신 기준이므로 내용이 충돌하면 `PRD.md`를 우선한다. (예: 기구/성능개발 병렬·직렬 설정 단위가 `prd_lite.md`에서는 "모델별", `PRD.md`에서는 "사양그룹별"로 다르게 적혀 있음 — `PRD.md` 기준인 "사양그룹별"이 맞다.)

## 이 앱은 무엇인가

신모델 개발일정을 관리하는 담당자(PM/일정 담당자)가 50개 이상의 모델을 대상으로, 사양그룹·출향지·법인 조건에 따라 달라지는 소요 시간을 반영해 개발 순서와 일정을 **자동으로 계산**해주는 사내 업무 시스템. 개인정보는 다루지 않으며 사내 기밀(Confidential) 등급 데이터.

## 기술 스택

- **Next.js** — 프론트엔드와 API를 한 프로젝트에서 처리, Vercel 배포 예정
- **Supabase (PostgreSQL)** — 모델 정보, 시험/규격 마스터 테이블 등 저장
- **접근 통제**: 로그인 계정 없이 단일 비밀번호로 접근 통제 (최초 설정 후 화면에서 변경 가능)
- `.env`에 `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `OPENAI_API_KEY` 보관, `.gitignore`로 저장소에서 제외됨

## 핵심 도메인 로직 (일정 자동 계산 규칙)

이 앱의 핵심은 자동 일정 수립 알고리즘이며, 여러 조건이 얽혀 있어 구현 시 반드시 아래 규칙을 지켜야 한다 (`PRD.md` 5절 참고):

- **기구개발·성능개발**: 사양그룹별로 설정된 병렬/직렬 여부에 따라 순서 계산. 소요 시간(일 단위)은 모델별 사용자 입력값.
- **신뢰성 시험**: 기본은 병렬 진행, 직렬로 표시된 소수 항목만 순서를 반영. 소요 시간(일 단위)은 시험 마스터 테이블 기준.
- **규격취득**: 신뢰성 시험과 항상 병렬 진행. 소요 시간(주 단위)은 규격 마스터 테이블의 출향지별 값 기준. "규격취득 필요" 표기된 모델에만 일정 반영.
- **개발 완료 조건**: 신뢰성 시험 + 규격취득이 모두 끝나야 완료. 완료 판정은 두 작업이 모두 끝나는 날짜가 속한 주(월요일 시작 기준)로 Capa에 반영.
- **일정 기준일**: 사용자가 입력한 시작 기준일부터 계산. 한국 공휴일에는 개발(기구/성능개발) 및 신뢰성 시험을 진행하지 않는 것으로 계산.
- **법인 Capa 제한**: 한 주에 개발 완료되는 모델 수는 소속 법인의 주간 Capa(고정값)를 초과할 수 없음. 초과 예상 시 ① 담당자가 지정한 사양그룹 우선순위 → ② 동일 그룹 내 출향지 우선순위 순으로 정렬해 배치. Capa로 인해 전체 일정이 길어져도 결과는 제한 없이 모두 표시.
- **재계산**: 조건(모델 추가, 우선순위 변경 등) 변경 시 전체 일정을 다시 계산하고, 이전/이후 일정을 나란히 비교할 수 있는 화면을 제공하며 변경된 부분은 별도 색상으로 표시.
- 일정 결과, 모델 정보, 마스터 테이블은 모두 엑셀로 내보내기 가능해야 함.

### 데이터 모델 관련 제약

- 모델 필수 입력: 그룹(사양), 출향지, 법인 — 하나라도 비어 있으면 자동 일정 수립 실행 자체를 막아야 함.
- 사양그룹·법인은 담당자가 화면에서 자유롭게 추가/관리 (개수 고정 아님, 법인은 현재 5개).
- 사양그룹·출향지에는 각각 담당자가 우선순위 번호를 직접 지정.
- 신뢰성 시험/규격취득 소요 시간은 모델에 직접 입력하지 않고 별도 마스터 테이블(시험 마스터, 규격 마스터)에서 관리하며, 모델은 필요한 항목을 선택해 적용.
- 법인별 Capa는 고정값으로 별도 관리, 법인 선택 시 자동 적용.
- 마스터 테이블/모델 정보는 엑셀 붙여넣기(Ctrl+V) 또는 파일 첨부로 일괄 입력 가능해야 함.
- 일괄 등록 시 필수 항목 누락 모델이 있으면 경고 표시 후 전체 등록을 막아야 함.
- 한 모델에 동일한 신뢰성 시험 항목이 중복 입력되면 에러 팝업을 띄우고 등록을 막아야 함.

## 범위 외 (구현하지 말 것)

- 실제 개발 진행 현황 추적/실적 입력 (계획 수립까지만, 진행 관리 제외)
- 로그인/계정 권한 관리 (단일 비밀번호 게이트, 단일 사용자 기준)
- 일정 변경 알림, 메일/메신저 발송
- 모바일 대응, 다국어 지원

## 개발 순서 (18개 단위, `PRD.md` 하단 참고)

1. Next.js 프로젝트 초기 설정 및 Supabase 연동
2. 단일 비밀번호 접근 통제 구현
3~6. 법인/사양그룹/신뢰성 시험/규격 마스터 등록·조회 기능
7~8. 모델 정보 입력 화면, 신뢰성 시험 항목 선택 및 규격취득 필요 여부 표기
9~11. 엑셀 붙여넣기/파일 첨부 일괄 입력, 필수 항목 누락 검증, 중복 시험 항목 검증
12~15. 시작 기준일·공휴일 반영, 병렬/직렬 소요 시간 계산 로직, 신뢰성 시험·규격취득 소요 시간 계산 로직, 법인 Capa 기반 제한/우선순위 로직
16. 전체 모델 자동 일정 수립 실행 및 결과 목록 화면
17. 조건 변경 시 재계산 및 이전/이후 비교 화면(변경분 색상 표시)
18. 일정/모델/마스터 테이블 엑셀 내보내기
