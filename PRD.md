# [프로젝트 이름] 서비스 기획서 (PRD)

- 작성자: 서정용
- 작성일: 2026-08-18

---

## 1) 추진 배경 (문제 정의)
신모델 개발일정을 관리하는 담당자(PM/일정 담당자)는 프로젝트 내 50개 이상의 다양한 모델을 대상으로, 사양·출향지 등 여러 조건에 따라 달라지는 소요 시간을 고려하여 개발 순서와 일정을 수립해야 한다. 이 과정을 수작업으로 계산하면서 겪는 비효율과 오류 가능성이 이번 시스템 개발의 배경이다.

## 2) 현행 방식과 한계
- 현재는 담당자가 엑셀 등으로 모델별 사양·출향지·법인 조건을 직접 대조하며 소요 시간을 계산하고, 개발 순서와 일정을 수작업으로 배치하고 있다.
- 모델 수가 50개를 넘어가면서 기구/성능개발의 병렬·직렬 여부, 신뢰성 시험 항목별 소요 시간, 규격취득 조건, 법인별 Capa 제한까지 사람이 동시에 고려하기 어려워 계산 누락이나 오류가 발생하기 쉽다.
- 조건이 바뀌면(예: 모델 추가, 우선순위 변경) 전체 일정을 처음부터 다시 계산해야 해서 시간이 많이 든다.

## 3) 목표와 기대효과 (성공 기준)
- 담당자 1인이 50개 이상 모델의 개발 일정을 하루 이내에 산출할 수 있도록 한다.
- 수작업 대비 일정 수립 소요 시간을 50% 이상 단축한다.
- 법인별 주간 Capa를 초과하는 일정 오류를 0건으로 만든다.
- 모델 조건(그룹/출향지/법인 등) 변경 시 재계산 소요 시간을 수 분 이내로 단축한다.
- 목표 완료일: 2026-08-19 (오늘, 개발 단위 18개 전체 완료 목표)

## 4) 사용자와 이용 흐름
- 사용자: 신모델 개발일정을 관리하는 담당자(PM/일정 담당자)
- 이용 흐름: 모델 정보 입력(그룹, 출향지, 법인, 개발 소요 시간 등) → 일정 시작 기준일 입력 → 법인별 Capa 확인 → 자동 일정 수립 실행 → 생성된 개발 순서·일정 확인 → 조건 수정 시 전체 일정 재계산(이전 대비 변경된 부분은 별도 색상으로 표시)

## 5) 주요 기능 (must / nice로 구분)

### Must
**1) 자동 일정 수립**
- 설명: 그룹(사양)별 기구개발·성능개발(병렬/직렬), 품질 신뢰성 시험(시험 마스터 테이블 기준 항목별 소요시간·병렬/직렬 구분), 규격취득(신뢰성 시험과 병렬 진행, 규격 마스터 테이블 기준 출향지별 소요시간)을 반영해 전체 모델의 개발 순서와 일정을 자동으로 생성. 모델이 속한 법인의 Capa에 따른 주간 개발완료 가능 모델 수 제한도 함께 반영.
- AI가 꼭 지켜야 할 규칙:
  - 기구개발·성능개발은 사양그룹별로 설정된 병렬/직렬 여부에 따라 순서를 다르게 계산해야 한다
  - 기구개발(설계), 성능개발(성능 검토·진동소음 검토) 소요 시간은 사용자가 사양그룹별로 입력한 값(일 단위)을 기준으로 계산해야 한다
  - 신뢰성 시험 항목은 기본적으로 병렬로 진행하되, 직렬로 표시된 소수 항목은 순서를 반영해 전체 소요 시간을 계산해야 한다 (소요 시간은 시험 마스터 테이블 값 기준, 일 단위)
  - 규격취득은 신뢰성 시험과 항상 병렬로 진행되고, 규격 마스터 테이블의 출향지별 값(주 단위)을 기준으로 소요 시간을 계산해야 한다
  - 규격취득이 필요한 모델로 표기된 경우에만 해당 모델의 일정에 규격취득 기간을 반영해야 한다
  - 신뢰성 시험과 규격취득이 모두 끝나야 해당 모델의 개발이 완료된 것으로 처리한다
  - 개발 완료 여부는 신뢰성 시험과 규격취득이 모두 끝나는 날짜가 속한 주(월요일 시작 기준)를 기준으로 Capa에 반영해야 한다
  - 일정은 사용자가 입력한 시작 기준일부터 계산해야 한다
  - 한국 공휴일(달력 기준)에는 개발(기구개발·성능개발) 및 신뢰성 시험을 진행하지 않는 것으로 계산해야 한다
  - 한 주에 개발 완료되는 모델 수는 해당 모델이 속한 법인의 Capa 값을 초과하지 않아야 하며, 초과가 예상되면 담당자가 지정한 사양그룹 우선순위로 먼저 정렬하고, 동일 그룹 내에서는 담당자가 지정한 출향지 우선순위로 순서를 결정해야 한다
  - 법인 Capa로 인해 전체 일정이 길어지더라도, 계산된 결과를 제한 없이 모두 표시해야 한다
  - 조건 변경 시 일정은 전체를 다시 계산하며, 이전 일정과 이후 일정을 나란히 비교할 수 있는 화면을 제공하고, 변경된 부분은 별도 색상으로 표시해야 한다
  - 생성된 개발 일정은 엑셀 파일로 내보낼 수 있어야 한다

**2) 모델 정보 입력/관리**
- 설명: 그룹(사양), 출향지, 법인 등 모델별 기본 정보와 개발 소요 시간, 신뢰성 시험/규격취득 마스터 테이블, 법인별 Capa(고정값), 일정 시작 기준일을 등록·수정
- AI가 꼭 지켜야 할 규칙:
  - 모델당 그룹, 출향지, 법인을 필수 입력 항목으로 받는다
  - 사양그룹은 담당자가 화면에서 자유롭게 추가·관리할 수 있어야 하며, 그룹 수는 고정하지 않는다 (과제별로 그룹 수가 달라질 수 있음)
  - 사양그룹별로 기구개발·성능개발의 병렬/직렬 진행 방식과 기구개발(설계)·성능개발(성능 검토·진동소음 검토) 소요 시간(일 단위)을 설정할 수 있어야 한다
  - 신뢰성 시험 항목별 소요 시간(일 단위)은 별도의 시험 마스터 테이블에서 관리하고, 모델은 필요한 항목을 선택해 적용한다
  - 출향지별 규격취득 소요 시간(주 단위)은 별도의 규격 마스터 테이블에서 관리한다
  - 모델 목록에서 규격취득이 필요한 모델을 별도로 표기할 수 있어야 한다
  - 규격 마스터는 출향지 1차원 기준으로만 관리한다 (그룹×출향지 조합별 소요시간 차등, 대표 모델 1개만 취득하면 되는 케이스는 이번 범위에서 제외하며, 모델별 규격취득 필요 여부는 모델 목록의 개별 표기로 판단한다)
  - 일정 계산의 시작 기준일을 사용자가 입력할 수 있어야 한다
  - 법인은 담당자가 화면에서 자유롭게 추가·관리할 수 있어야 한다 (현재 기준 5개 법인)
  - 사양그룹과 출향지에는 각각 담당자가 우선순위 번호를 직접 지정할 수 있어야 한다
  - 법인별 Capa는 고정값으로 별도 관리하며, 법인을 선택하면 해당 법인의 Capa가 자동으로 적용되어야 한다
  - 필수 항목이 비어있으면 자동 일정 수립을 실행할 수 없게 막는다
  - 마스터 테이블(신뢰성 시험, 규격 등)과 모델 정보는 엑셀에서 복사한 데이터를 붙여넣기(Ctrl+V)하거나 엑셀 파일을 첨부하는 방식으로 일괄 입력할 수 있어야 한다
  - 모델 정보를 일괄 등록할 때 필수 항목이 누락된 모델이 있으면 경고 문구를 표시하고 전체 등록을 막아야 한다
  - 모델에 동일한 신뢰성 시험 항목이 중복으로 입력되면 에러 팝업을 띄우고 등록을 막아야 한다
  - 모델 정보와 마스터 테이블 데이터는 엑셀 파일로 내보낼 수 있어야 한다

### Nice
- 해당 없음 (이번 범위에서는 정의하지 않음)

## 6) 범위와 비범위

### 범위 (이번 실습에서 만들 것)
- 자동 일정 수립
- 모델 정보 입력/관리

### 비범위 (이번 실습에서 만들지 않을 것)
- 실제 개발 진행 현황 추적/실적 입력 기능 (계획 수립까지만, 진행 관리는 제외)
- 사용자 권한/계정 관리 기능 (로그인 없이 단일 비밀번호로 접근 통제, 단일 사용자 기준으로 우선 개발)
- 일정 변경 알림, 메일/메신저 발송 기능
- 모바일 대응, 다국어 지원

## 7) 보안·개인정보 검토
- 공개등급: 사내 기밀/내부용 (Confidential) — 모델 사양·개발일정 정보는 외부에 공개하지 않는다.
- 접근 통제: 별도 로그인 계정 없이, 단일 비밀번호 입력 방식으로 접근을 통제한다. 비밀번호는 담당자가 최초 사용 시 설정하고, 이후 화면에서 직접 변경할 수 있어야 한다.
- 개인정보 여부: 모델 사양·출향지·일정 데이터가 중심이며 개인정보는 포함하지 않는다.
- API 키 관리: GITHUB_TOKEN, SUPABASE_ACCESS_TOKEN, VERCEL_TOKEN, OPENAI_API_KEY 등은 `.env` 파일에 보관하고, `.gitignore`에 `.env`를 등록하여 저장소(GitHub)에 올라가지 않도록 한다.

## 8) 기술 스택
- Next.js — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- 고정 이유: Part 1~2 실습에서 쓴 것과 같은 구성이고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
- Supabase (PostgreSQL) — 모델 정보, 시험/규격 마스터 테이블 등 데이터 저장 및 관리
- 고정 이유: 별도 서버 구축 없이 바로 연동할 수 있고, Vercel 배포와 궁합이 좋음

## 개발 단위
1. Next.js 프로젝트 초기 설정 및 Supabase 연동
2. 단일 비밀번호 접근 통제(로그인 없는 패스워드 게이트, 최초 설정·변경 기능) 구현
3. 법인 마스터(법인명, 주간 Capa) 등록/조회 기능 개발
4. 사양그룹 마스터(그룹명, 기구·성능개발 병렬/직렬 설정) 등록/조회 기능 개발
5. 신뢰성 시험 마스터(항목명, 소요 시간, 병렬/직렬 여부) 등록/조회 기능 개발
6. 규격 마스터(출향지별 규격취득 소요 시간) 등록/조회 기능 개발
7. 모델 정보 입력 화면(그룹, 출향지, 법인, 기구/성능개발 소요 시간) 개발
8. 모델별 신뢰성 시험 항목 선택 및 규격취득 필요 여부 표기 기능 개발
9. 붙여넣기(Ctrl+V) 및 엑셀 파일 첨부를 통한 마스터/모델 데이터 일괄 입력 기능 개발
10. 모델 일괄 등록 시 필수 항목 누락 검증(경고 표시·등록 차단) 개발
11. 모델별 동일 신뢰성 시험 항목 중복 입력 검증(에러 팝업) 개발
12. 일정 시작 기준일 입력 및 한국 공휴일 캘린더 반영 로직 개발
13. 사양그룹 병렬/직렬 설정을 반영한 기구개발·성능개발 소요 시간 계산 로직 개발
14. 신뢰성 시험·규격취득 병렬 진행 소요 시간 계산 로직 개발
15. 법인 Capa 기반 주간 완료 모델 수 제한 및 우선순위(사양그룹→출향지) 적용 로직 개발
16. 전체 모델 자동 일정 수립 실행 및 결과 목록 화면 개발
17. 조건 변경 시 전체 재계산, 이전/이후 일정 나란히 비교 화면 및 변경분 색상 표시 기능 개발
18. 일정 결과·모델 정보·마스터 테이블 엑셀 내보내기(다운로드) 기능 개발
